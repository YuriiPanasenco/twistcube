
export default class Rotator {

    /**
     * made the rotation
     * @param clockwise=true
     * @abstract
     */
    rotate(clockwise=true){}

    /**
     * the method need to show a screen of animations
     * @abstract
     */
    update(){}
}
